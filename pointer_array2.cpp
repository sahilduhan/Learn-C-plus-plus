#include<bits/stdc++.h>
using namespace std;
int main()
{

char c[] ="sahil";
int len=strlen(c);
int size=sizeof(c);

cout<<"the length is "<<len<<" and the size in bytes is "<<size<<endl;

return 0;

}